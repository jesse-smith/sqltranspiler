A simple web app using the sqlglot Python package to convert between SQL dialects.

Simply paste your query, choose its dialect, and choose the dialect you’d like to transpile into.

The application runs entirely in browser - Pyodide is used to install and run sqlglot.